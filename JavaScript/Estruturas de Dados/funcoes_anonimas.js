//Funções que não possuem nome, e só podem ser acessadas depois de serem inicializadas

const somar = function (a, b) {
  return a + b;
};

const subtrair = function (a, b) {
  return a - b;
};

console.log(somar(4, 5));
console.log(subtrair(4, 5));
